name: hr_portal
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:

  flutter_localizations:
    sdk: flutter
    
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ── Network (Data Layer — DO NOT CHANGE) ──
  dio: ^5.9.1
  flutter_secure_storage: ^10.0.0
  get_it: ^9.2.1
  equatable: ^2.0.8
  uuid: ^4.5.3

  # ── State Management ──
  flutter_riverpod: ^3.2.1

  # ── Routing ──
  go_router: ^17.1.0

  # ── Utilities ──
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:

  uses-material-design: true

